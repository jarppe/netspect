name: Docker Image CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - name: Build and push
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: ghcr.io/jarppe/netspect:latest,ghcr.io/jarppe/netspect:$(date +%Y-%m-%d-'T'-%H-%M-%S)
